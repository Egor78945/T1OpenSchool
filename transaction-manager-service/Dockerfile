ARG JAVA_VERSION
ARG TRANSACTION_MANAGER_SERVICE_PORT_OUTER

FROM openjdk:$JAVA_VERSION

COPY build/libs/*.jar app.jar

EXPOSE $TRANSACTION_MANAGER_SERVICE_PORT_OUTER

ENTRYPOINT ["java", "-jar", "app.jar"]